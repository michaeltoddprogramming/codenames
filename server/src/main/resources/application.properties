# Fill in your Google Cloud Console desktop client ID
google.client-id=592136454918-66oe1so2i84cg3n5jiktl70er4vpmege.apps.googleusercontent.com

# Must be at least 32 characters
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=86400000

server.port=8080

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Developer-controlled global lobby match duration (minutes)
app.game.match-duration-minutes=5
